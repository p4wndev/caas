import requests
import time

API_KEY = 'VF.DM.65929cdabb70aa0008e6c814.Z7B59nlaj2X1os09'
def api_data(prompt):
    # voiceflow_start_time = time.time()
    # url = 'https://general-runtime.voiceflow.com/knowledge-base/query'
    # headers = {
    #     'Authorization': API_KEY,
    #     'Accept': 'application/json',
    #     'Content-Type': 'application/json'
    # }

    # data = {'question': prompt}

    # response = requests.post(url, headers=headers, json=data)

    # text = response.json().get('output')
    # print(text)
    # voiceflow_elapsed_time = time.time() - voiceflow_start_time
    # print(f"Thời gian chạy Voiceflow: {voiceflow_elapsed_time} seconds")
    # if text != None:
    #     return text
    # else: return ""
    return ""
